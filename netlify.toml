[build]
  base    = "02_Frontend"
  command = "npm run build"
  publish = "02_Frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes served by index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
